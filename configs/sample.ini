[COMMON]
seed = 42
work_dir = 'work_dirs'

[DATA]
dataset = 'PKUMMD' # Supported datasets: 'OAD', 'PKUMMD'
batch_size = 16
num_workers = 0
in_channels = 3
num_classes = 52
num_frames = 16
num_nodes = 25
data_dir = 'C:\\Users\\user\\Desktop\\PKUMMD'

[MODEL]
model = 'GATv2' # Supported models: 'GATv2'
in_channels = 3
hidden_channels = 128
out_channels = 52
num_heads = 4
dropout = 0.5

[TRAINING]
total_epoch = 30
learning_rate = 0.001
early_stopping = True
patience = 10

[LOSS]
loss_fn = 'CrossEntropyLoss'
loss_kwargs = {}

[OPTIMIZER]
optimizer = 'AdamW'
optimizer_kwargs = {'weight_decay': 1e-4}

[SCHEDULER]
scheduler = 'ReduceLROnPlateau'
scheduler_kwargs = {'mode': 'min', 'factor': 0.1, 'patience': 10}

[LOGGING]
log_level = 'INFO' # Supported levels: 'DEBUG', 'INFO', 'WARNING', 'ERROR'

[EVAL]
exp_dir = 'work_dirs\PKUMMD_GATv2_20260304_133320'
metrics = ['accuracy', 'mean_class_accuracy', 'action_accuracy'] # Supported metrics: 'accuracy'